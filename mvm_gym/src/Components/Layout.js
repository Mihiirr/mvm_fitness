import React, { Children } from 'react'

const Layout = () => {
    return (
        <div>
            {/* Header */}
            <div> headers</div>

            {/* Body */}
            <div>{Children}</div>

            {/* Footer */}
            <div></div>
        </div>
    )
}

export default Layout